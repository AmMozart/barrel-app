import React from 'react';
import style from './Footer.module.css'

const Footer = (() => {
  return (
    <footer className={`footer ${style.alarm}`}>
      <h2>Предупреждение</h2>
    </footer>
  );
}
);

export default Footer;
